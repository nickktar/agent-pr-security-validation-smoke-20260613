// Simulated agent-generated change for security validation smoke test.
// No real secrets are included in this file.
import _ from 'lodash';

export function uniqueValues(values) {
  return _.uniq(values);
}
